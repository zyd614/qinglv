PORT=3000
SITE_URL=https://your-domain.example

# Change these before deploying. Defaults only exist for local preview.
FEMALE_PASSWORD=moon-0820
MALE_PASSWORD=forest-1124
ADMIN_PASSWORD=together-520

# Optional SMTP. When configured, the other person gets an email for every new message.
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-mail@example.com
SMTP_PASS=your-smtp-password
MAIL_FROM=your-mail@example.com
FEMALE_EMAIL=
MALE_EMAIL=